# The committed design-system export (Claude Design) is reference material, not app source.
# Mark it vendored so GitHub's language stats reflect the actual app code (TypeScript).
.claude/** linguist-vendored
