Single text field — 48px tall, white on ivory, brown border + soft ring on focus.

```jsx
<Input label="이메일" placeholder="galpi@example.com" />
<Input iconLeft="search" placeholder="책 제목이나 작가를 검색해보세요" />
<Input label="비밀번호" type="password" error="비밀번호가 6자 이상이어야 해요" />
<Input as="textarea" label="메모" rows={4} />
```
