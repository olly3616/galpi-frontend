Bare icon-only tap target for headers, card corners and toolbars.

```jsx
<IconButton icon="bell" label="알림 설정" active />
<IconButton icon="chevron-left" label="뒤로" />
```

No background by default; `active` tints to brown. Keep `size` ≥ 40 in app screens.
