White surface on the ivory page — the base container for anything sitting on the background.

```jsx
<Card><p className="galpi-body">서재에 담은 기록</p></Card>
<Card padded={false} elevated={false}>{/* flush list row */}</Card>
```

Always 16px radius + hairline border; shadow stays at --shadow-2 or lighter.
