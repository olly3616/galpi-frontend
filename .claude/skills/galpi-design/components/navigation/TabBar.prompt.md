Bottom navigation for the logged-in app: 내 책장 / 피드 / 프로필. Selected tab is brown.

```jsx
<TabBar active="feed" onChange={setTab} />
```

Three tabs only — the default set matches the spec; don't add a fourth.
